import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  button: {
    borderColor: "black",
    padding: 10,
    marginVertical: 15,
    backgroundColor: "deepskyblue",
  },
});
